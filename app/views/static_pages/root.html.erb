<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(partial: "api/users/user", formats: :json,
      handlers: :jbuilder, locals: { user: current_user }).html_safe %>
  </script>
<% end %>

<main id="root"></main>